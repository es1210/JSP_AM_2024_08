package com.KoreaIT.java.jsp.AM;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import java.io.IOException;

@WebServlet("/printDan")
public class PrintDanServlet extends HttpServlet {

	protected void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		response.setContentType("text/html;charset=UTF-8");

		// URL 파라미터에서 dan 값을 가져옵니다.
		String danParam = request.getParameter("dan");
		int dan = 8; // 기본값으로 8단을 설정합니다.

		// dan 파라미터가 null이 아니고, 숫자인지 확인합니다.
		if (danParam != null && danParam.matches("\\d+")) {
			dan = Integer.parseInt(danParam);
		}

		response.getWriter().append(String.format("==%d단==<br>", dan));

		for (int i = 1; i <= 9; i++) {
			response.getWriter().append(String.format("%d * %d = %d<br>", dan, i, dan * i));
		}
	}
}
